import { forwardRef } from "react";
import type { UnfoldHandle, UnfoldProps } from "./types";

/** Phase 1 stub: type-checks the full public prop surface but renders nothing.
 *  The R3F scene is wired in Phase 2. */
export const Unfold = forwardRef<UnfoldHandle, UnfoldProps>(function Unfold(
  _props,
  _ref,
) {
  return null;
});
